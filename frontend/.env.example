# Frontend API base URL
# Local dev should use Vite proxy via /api/v1
VITE_API_BASE_URL=/api/v1

# Production example (set this in your hosting env vars instead of committing secrets)
# VITE_API_BASE_URL=https://nodue-cdgi-major.onrender.com/api/v1
